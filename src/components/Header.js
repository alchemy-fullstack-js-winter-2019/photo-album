import React from 'react';
import styles from './Header.css';

function Header() {
  return (
    <header className={styles.header}>
      <h1>Where my dawgs at</h1>
    </header>
  );
}

export default Header;
